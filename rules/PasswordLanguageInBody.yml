source: |-
  type.inbound
  and regex.icontains(body.current_thread.text,
                    '(retain same password)'
  )
type: detection_rule
name: Password language in body
authors:
- name: NDefend
description: Rule to detect the presence of a string to identify bad.
references: []
tags:
- Brand impersonation
severity: medium
